import React from 'react'
import PropTypes from 'prop-types'
import moment from "moment";

function List({title, name, id, price, product_image, star, description, created_at}){

  return(
    <div>
    
    </div>
  )
}

export default List;
